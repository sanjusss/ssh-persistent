# 真实主机配置(含明文密码),只提交 hosts.conf.example
hosts.conf

# python 缓存
__pycache__/
*.pyc

# 运行期日志和错误文件
*.log
*.err

# 套接字和进程号文件(默认在 /tmp,这里防御用户把 SSH_MUX_SOCKET_DIR 指向仓库目录)
*.sock
*.pid

# 用户私有运行目录，包含控制接口、文件锁及无扩展名的 SSH 套接字
ssh-mux-[0-9]*/

# 桌面系统生成的文件
.DS_Store
Thumbs.db
